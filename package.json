{
  "name": "budget-tracker",
  "version": "1.0.0",
  "description": "Personal budget tracker with Plaid — deploys to Netlify",
  "scripts": {
    "dev":   "netlify dev",
    "local": "node server.js"
  },
  "dependencies": {
    "dotenv":     "^16.4.5",
    "express":    "^4.19.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
